# Server
PORT=3000
NODE_ENV=development

# PostgreSQL (connection string)
DB_URI=postgresql://user:password@localhost:5432/studysync
DB_SSL=false

# JWT
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=7d
